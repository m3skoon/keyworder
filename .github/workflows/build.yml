name: Build Windows EXE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install anthropic pillow pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --onefile --noconsole --name Keyworder `
            --hidden-import anthropic `
            --hidden-import PIL `
            --hidden-import PIL.Image `
            --hidden-import PIL.ImageOps `
            keyworder.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Keyworder-Windows
          path: dist/Keyworder.exe
          retention-days: 30
